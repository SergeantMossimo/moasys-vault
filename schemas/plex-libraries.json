{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://moasys.local/vault/schemas/plex-libraries.json",
  "title": "MOASYS-Vault — output/plex/libraries.json",
  "description": "Index of every library on the Plex server, including photo libraries that aren't pulled. Libraries skipped by a partial pull keep their counts from the previous pull. Generated by `npm run plex:pull`.",
  "type": "object",
  "properties": {
    "generated": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 UTC timestamp of when the file was written."
    },
    "server": {
      "type": "object",
      "properties": {
        "machine_identifier": { "type": "string" },
        "version": {
          "type": "string",
          "description": "Plex Media Server version."
        }
      },
      "required": ["machine_identifier", "version"],
      "additionalProperties": false
    },
    "libraries": {
      "type": "array",
      "items": { "$ref": "plex-common.json#/$defs/Library" }
    }
  },
  "required": ["generated", "server", "libraries"],
  "additionalProperties": false
}
